The OR_DS_SDS_Solution_Anup.zip contains all the codes and results files.
Else main.py and optMIPFunc.py are files for the MIP approach while the ga_approach.py is for GA approach.
ResultsForBothApproach.zip contains the outputs
